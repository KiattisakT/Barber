# Frontend API settings
# Local dev can keep /api because Vite proxies it to http://localhost:4000.
VITE_API_BASE_URL=/api
VITE_SHOP_SLUG=dream-catcher
VITE_DEFAULT_STAFF_ID=staff_arm

# Demo seed date used by the current prototype data.
VITE_QUEUE_DATE=2026-06-17
